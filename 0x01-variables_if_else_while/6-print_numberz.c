#include <stdio.h>

/**
 * main - Prints all single digit numbers of base 10
 * Return: 0 (complete)
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
