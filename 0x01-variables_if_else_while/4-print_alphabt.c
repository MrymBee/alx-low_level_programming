#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 * Return: 0 (complete)
 */
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}

